-- CREATE DATABASE
CREATE DATABASE `DATA` DEFAULT CHARACTER SET UTF8;

-- DATA_TB
CREATE TABLE `DATA`.`DATA_TB` (
	`NUM`   INT   NOT NULL, -- NUM
	`YEAR`  INT   NOT NULL, -- YEAR
	`MAN`   INT   NOT NULL, -- MAN
	`WOMAN` INT   NOT NULL, -- WOMAN
	`TOTAL` INT   NOT NULL, -- TOTAL
	`RATIO` FLOAT NOT NULL  -- RATIO
);

-- DATA_TB
ALTER TABLE `DATA`.`DATA_TB`
	ADD CONSTRAINT `PK_DATA_TB` -- DATA_TB �⺻Ű
		PRIMARY KEY (
			`NUM` -- NUM
		);

ALTER TABLE `DATA`.`DATA_TB`
	MODIFY COLUMN `NUM` INT NOT NULL AUTO_INCREMENT;
